package admission

import (
	"context"
	"log"
)

func logMultiUniverseTransition(ctx context.Context) error {
	log.Println("initializing states in multiple universes (Payment, Signature and Form)")
	return nil
}
